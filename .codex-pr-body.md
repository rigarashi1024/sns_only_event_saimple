## Summary

- Codex skill 実行時の escalation 方針を明記
- `.git` 書き込みや GitHub API を使う skill は、最初から sandbox 外実行する運用に変更

## Changes

- `sync-main` は通常 sandbox だと `.git/FETCH_HEAD` 書き込みで失敗しやすいため、初回から escalation 実行するよう明記
- `pr-creator` / `fix-loop` は GitHub API と git 書き込みを行うため、初回から escalation 実行するよう明記
- `preflight` は package manager / network が必要な環境では初回から escalation 実行する方針を追記

## Validation

- [x] `bash scripts/codex-preflight.sh`

## Review Notes

- 実装コード変更はなく、Codex skill の運用文書のみの変更です
